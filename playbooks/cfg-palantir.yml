---

- hosts: palantir

  tasks:
    - name: Install packages
      apt:
        name:
          - nginx
      become: true

    - name: Copy nginx confs
      copy:
        src: /repos/serenity/config/palantir/nginx/
        dest: /etc/nginx/
      become: true
      notify: "restart nginx"

    - name: Sync nginx data directory
      ansible.posix.synchronize:
        src: /home/aragorn/dist/data/
        dest: /data/
      become: true
      notify: "restart nginx"
  
  handlers:
    - name: restart nginx
      systemd:
        name: nginx
      become: true
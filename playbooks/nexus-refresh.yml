---

- hosts: localhost

  roles:
    - role: user
      setup_user: strider
    - role: nexus
    - role: pihost
    - role: sshd
      in_if: eth0
    - role: samba
    - role: avahi
    - role: fw_default
      in_if: eth0
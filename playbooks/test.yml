---

- hosts: localhost

  tasks:

    - name: Send test mail
      community.general.mail:
        host: "{{ hostvars['mail'].ansible_host }}"
        password: DRCYrAyM^Lt$9e
        username: webmeowster@spencers.house
        sender: webmeowster@spencers.house
        port: 587
        to: srsnowden@gmail.com
        subject: Hello pt. 3
        body: Hello from ansible